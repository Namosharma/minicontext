import React from "react";

const UserContext = React.createContext(); //createcontext method hota hai

export default UserContext;
//context ko global variable ki tarah jaano

//we also have to make provider
